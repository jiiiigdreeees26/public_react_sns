// import commentReducer, { addComment } from "../commentSlice";

test.skip("addCommentを実行するとポストが追加される", () => {
  const initialState = {
    comments: []
  };
  // const newState = commentReducer(initialState, addComment({
  //   content: 'コメント',
  //   postId: 5001,
  //   userId: 502
  // }));
  // expect(newState.comments[0].id).toBe(1);
  // expect(newState.comments[0].content).toBe('コメント');
  // expect(newState.comments[0].postId).toBe(5001);
  // expect(newState.comments[0].userId).toBe(502);
});
